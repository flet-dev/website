{
  "name": "flet-dev",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "view": "serve -s build",
    "dev": "serve -s build",
    "bdev": "tool/prior dev && yarn build",
    "bpub": "tool/prior pub && yarn build",
    "perfix": "node tool/addperfix",
    "dev-all": "tool/prior dev && yarn build && yarn view",
    "pub-all": "tool/prior pub && yarn build && yarn perfix && yarn view"
  },
  "dependencies": {
    "@docusaurus/core": "^2.4.0",
    "@docusaurus/plugin-client-redirects": "^2.4.0",
    "@docusaurus/preset-classic": "^2.4.0",
    "@hcaptcha/react-hcaptcha": "^1.0.0",
    "clsx": "^1.1.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "wrangler": "^0.0.7"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
